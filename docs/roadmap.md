# Roadmap

IsaacMetalBridge is an unofficial experimental compatibility layer.
It is not affiliated with, endorsed by, sponsored by, or supported by Apple Inc. or NVIDIA Corporation.

## M0 — Foundation (completed)

- pinned Apple source and source-grounded integration analysis
- protocol v1 control plane
- Swift Metal capability host
- C++20 guest probe and local integration tests
- honest environment doctor/setup scripts

## M1 — Real guest and Isaac Sim failure capture (completed)

- Apple `container` 1.1.0 is installed outside the repository
- a real guest reports `aarch64`
- the user-managed official Isaac Sim 6.0.1 ARM64 image is loaded outside Git
- NVIDIA's real compatibility checker ran and returned `System checking result: FAILED`
- captured failures include missing `libcuda.so.1`, NVML, `nvidia-smi`, and Vulkan `ERROR_INCOMPATIBLE_DRIVER`

The original driver-boundary failure was used as the trace baseline for the startup compatibility work below.

## M2 — Production control transport (implemented and verified)

- Linux AF_VSOCK guest listener on an explicit port
- macOS adapter using Apple container's verified `ContainerClient.dial`
- protocol 1.20 session verified across a real Apple-container ARM64 VM
- bounded framing and disconnect cleanup

Remaining hardening: fuzzing, connection timeout/cancellation, and an authentication threat model before any non-local use.

## M3 — Vulkan-to-Metal minimum compute path (prototype verified)

- completed below Vulkan: real `MTLBuffer` allocation, bounded upload/readback, one compute dispatch, and real command-buffer fence
- verified deterministic result: `[1,2,3,4] + 5 = [6,7,8,9]`, including across VM vsock
- completed discovery slice: Khronos Linux loader loads the IMB ICD, negotiates via vsock, and enumerates one Vulkan 1.0 physical/logical device and queue
- completed compute slice: Vulkan buffer/memory, descriptors, one exactly recognized `ADD_U32` SPIR-V module, compute pipeline, command buffer, queue submission, Vulkan fence, and readback map to the verified IMB/Metal path
- verified through the real Linux Vulkan loader and Apple-container VM: `[1,2,3,4] + 5 = [6,7,8,9]`
- completed startup compatibility additions include Kit-facing resources, descriptor indexing, timeline semaphores, query pools, and safe generic graphics submission while preserving the exact Metal compute path
- injected into the real Isaac Sim 6.0.1 ARM64 image and verified through the official base experience

The exact fixed-triangle rendering slice now produces and checks an actual image; this does not imply general or Isaac/RTX rendering.

## M4 — Rendering path

- completed: option-free RGBA8 image/texture mapping and full-image readback
- completed: exact build-generated vertex/fragment SPIR-V recognition
- completed: Vulkan render pass, framebuffer, fixed graphics pipeline, draw, queue submission, real Metal fence, pixel validation, and headless PNG output
- completed: exact tested Isaac Kit UI pipeline recognition, ring-buffer uploads, indexed draws, vertex offsets, scissor rectangles, BGRA8 texture sampling, and alpha blending
- completed: continuously captured real Kit UI frames displayed by a native macOS viewer
- completed: native pointer and keyboard input forwarding through Kit's real Carbonite input provider; Full Warehouse verification opens Create/Window menus, selects a real Mesh prim and populates Property, and opens/closes the Extensions Manager
- completed: pinned SPIRV-Cross tool, ABI-preserving software-FP64 buffer lowering, and protocol 1.14 live compute-pipeline creation; 203 of 204 real Isaac requests compile into `MTLComputePipelineState` on M4, with 198 execution-enabled and 5 still retained as compile-only
- completed: generic Metal compute dispatch with push constants, buffer/image descriptors, and Vulkan-format texel-buffer views; the Full trace executes supported pipelines and safely skips unsupported/partially-bound RTX submissions
- completed: KHR triangle BLAS and instance TLAS compatibility objects backed by real Metal acceleration structures
- completed: real Metal ray dispatch against real Kit scene BLAS geometry and synchronization into Kit's triple-buffered 1280×720 viewport images
- completed: non-black `--demo-scene` viewport inside the live Kit UI, with mouse menu interaction and Stage-search keyboard input verified end to end
- completed: early `omni.usd` startup-stage extension replaces Isaac Sim base's disabled positional-USD path; local demo and real NVIDIA `Simple Grid` roots are verified by their `/World` children
- completed: scene-state version 3 publishes visible USD Mesh triangle counts, local bounds, material flags, and world transforms; the ICD matches these against computed real BLAS bounds, applies the authored transforms, and excludes renderer-internal BLASes from the null-descriptor fallback TLAS
- completed: scene-state version 9 publishes real Mesh points, triangulated face indices, normalized authored corner normals, triangulated corner UVs, bounded RGBA8 base-color pixels, and direct roughness/metallic/emission constants; the ICD deterministically builds one Metal BLAS per authored Mesh, with Cube and Ground verified together in a Full TLAS
- completed: direct bound `UsdPreviewSurface`/OmniPBR base color plus `primvars:displayColor` fallback transported per Mesh and consumed through Metal instance user IDs
- completed: follow connected `UsdUVTexture.outputs:rgb/rgba`; resolve the standard no-file fallback or decode a local file, resolve `UsdPrimvarReader_float2`, transport all five primvar interpolation modes, and barycentrically sample repeat-wrapped UVs in Metal; Full rendered the 8x8 checker PNG on Cube
- completed: Vulkan sparse-image memory requirements, mip storage, ordered buffer/image transfers, BC3 sRGB/BC5 UNORM uploads, and tile map/unmap mirrored into real Metal sparse textures and enabled by default
- completed: real R32_UINT texel-buffer dispatch plus buffer/image descriptor and readback transport
- completed: active Kit viewport camera plus the first USD SphereLight, DistantLight, and DomeLight published atomically by scene-state v4-v7 and used by protocol 1.14 Metal ray dispatch
- completed: NVIDIA Simple Grid's real `Wireframe_blue.png` sampled using its authored projected world-UV scale
- completed: active Kit viewport-camera scene trace into the 1280×720 Metal presentation image
- completed: `--camera-sensor-output` one-shot RGB/JSON contract resampled from that completed active-camera Metal frame; verified 921,600 RGB bytes and checksum in Full without a second failing Render Graph
- completed: real Kit timeline and CPU PhysX rigid-body gravity/contact slice using Isaac `DynamicCuboid`/`FixedCuboid`; Full reached the correct z=0.5 m resting center with MBP broadphase and emitted checked JSON
- completed: execute the seven real scalar/vector FP64-buffer requests through ABI-preserving binary64 storage conversion, compile all 34 matrix-typed requests with their padded 8-byte element layout preserved, and use the host translation result to enable the 21 whose selected entry point never evaluates the matrix
- completed: rerun Full Warehouse/Franka with the refined 190-enabled/13-compile-only gate; it reached `app ready`, published and built 824 Mesh/TLAS instances, produced the Metal scene frame, emitted none of the targeted Render Graph/multiTick/Sensor/Hydra/ray-rejection failures, and exited zero
- completed: harden early RTX SBT classification with an exact four-extent/four-raygen lookup that is accepted only when the linked pipeline contains one unique matching shader hash; add separate NV build/copy/trace, KHR copy, unresolved-SBT, and unknown-raygen rejection diagnostics
- completed: run the all-203-enabled diagnostic path both traced and untraced through Full Warehouse/Franka, `app ready`, 824-instance TLAS construction, a non-black frame, and zero targeted errors; none of the 13 true-use hashes were dispatched by those workloads, so all remained gated at that milestone
- completed: derive Vulkan descriptor-set/binding to compact Metal argument-ID maps from SPIR-V decorations/OpName plus translated MSL; all 1,270 argument fields in 202 captured compute modules map, sparse binding sets no longer use the wrong Metal slots, and same-binding type aliases fan out to every emitted ID
- completed: dispatch the actual captured `f5dd5704d7491f17` Isaac RTX world-transform shader on Apple M4 through the bridge, verify binary64 input `(1.5,-2.25,3.75)` in its output matrix, enable only that exact hash by default, and rerun Full Warehouse/Franka through `app ready`, ROS 2 Jazzy, the 824-instance TLAS, a checked non-black frame, zero targeted Render Graph/multiTick/Sensor/Hydra errors, and exit zero
- completed: protocol 1.14 transports independent Vulkan sampler state into argument-buffer-capable Metal samplers; dispatch the actual captured `64d94f5901ee0e4f` camera/depth shader with both samplers, verify its FP64-transformed clipping-plane pixel `[255,19,19,255]`, enable only that exact second hash, and rerun Full Warehouse/Franka through `app ready`, ROS 2 Jazzy, all 824 TLAS instances, a checked textured frame, zero targeted Render Graph/multiTick/Sensor/Hydra errors, and exit zero
- completed: dispatch the actual captured `75321ea922defdfc` panoramic depth-reconstruction shader on Apple M4, verify its software-FP64 world-position output pixel `[38,51,64,255]`, enable only that exact third hash, and rerun Full Warehouse/Franka through `app ready`, ROS 2 Jazzy, all 824 TLAS instances, a checked textured frame, and exit zero
- completed: dispatch the actual captured `361fde9aceebb12b` panoramic buffer-output variant on Apple M4, verify its software-FP64 storage-buffer world position `(1.5,2.0,2.5)`, and enable only that exact fourth hash
- completed: dispatch the actual captured `2eb5c9558c6d3e5a` motion-vector variant on Apple M4, verify its software-FP64 origin-difference pixel `[64,128,191,255]`, and enable only that exact fifth hash
- completed: dispatch the actual captured `610a7b9aed9f7b98` projected-depth variant on Apple M4, verify its software-FP64 depth pixel `[239,0,0,0]`, and enable only that exact sixth hash
- completed: rerun real Isaac Sim Full Warehouse/Franka with the six-hash default gate; the saved trace contains 196 execution-enabled pipelines, 7 compile-only FP64 pipelines, the expected single 64-bit-atomic rejection, ROS 2 Jazzy, `app ready`, 824 scene Meshes, an 824-instance Metal TLAS, a checked textured 1440x900 viewer frame, native pointer/keyboard forwarding, zero targeted Render Graph/multiTick/Sensor/Hydra errors, and exit zero
- completed: add RGBA8 UINT image transport and dispatch the actual captured `b258118a36125408` final-composite variant on Apple M4; its software-FP64 camera origin hits the clipping-plane early-output path and changes the initialized magenta pixel to `[0,0,0,255]`, enabling only that exact seventh hash
- completed: accept only identical persistent `vkMapMemory` ranges with reference-counted unmaps, recover SPIRV-Cross generated `m_ID` argument names from the original SPIR-V result ID, and pass the real Apple-container ICD probe through storage compute, texel compute, sparse images, raster, BLAS/TLAS, and Metal ray dispatch
- completed: defer resolved pure-KHR Isaac scene traces while the real USD Mesh manifest is still being published instead of rejecting the queue and poisoning Render Graph; the seven-hash Full Warehouse/Franka rerun logged 197 execution-enabled and 6 compile-only pipelines, loaded ROS 2 Jazzy, reached `app ready`, published 824 Meshes/67 textures, built the 824-instance Metal TLAS, produced a checked textured 1440x900 frame, reported zero targeted Render Graph/multiTick/Sensor/Hydra failures, and exited zero
- completed: add a backward-compatible ordinary 3D image encoding, exact 2x2x2 host/guest transfer tests, and sampled/writable 3D Metal argument binding; dispatch captured `c93eebdfc4dd964b` twice on Apple M4 and verify that changing only the binary64 camera-origin x value changes its 3D storage output from `[0,0,0,0]` to `[11,0,0,0]`, enabling only that exact eighth hash
- completed: preserve the established level-zero bridge behavior for mipmapped Full UI 2D textures while keeping ordinary 3D resources one-mip/one-layer; the real Full Warehouse/Franka rerun logged 198 execution-enabled and 5 compile-only pipelines, ROS 2 Jazzy, `app ready`, 824 Meshes and 824 TLAS instances, repeated real 1440x900 Kit UI frames, zero targeted Render Graph/multiTick/Sensor/Hydra/format errors, and exit zero
- completed: dispatch captured `bdd2d21d53978c2e` twice on Apple M4 with its real 2D/3D descriptor set and verify that changing only the binary64 camera-origin y value switches the volume-composite output from scaled `[128,0,128,255]` to clipped `[255,0,255,255]`; the nine-hash Full Warehouse/Franka rerun logged 199 execution-enabled and 4 compile-only pipelines, ROS 2 Jazzy, `app ready`, 824 Mesh/TLAS instances, a checked textured Full frame, zero targeted Render Graph/multiTick/Sensor/Hydra/format failures, and exit zero
- completed: dispatch the 62,000-line captured `d38fa4ca78558061` render-output shader twice on Apple M4 with its real UBO, texel-buffer, 2D/3D texture, sampler, and storage-image arguments; changing only the binary64 origin changes its checked RGBA8 output from `[64,128,191,255]` to `[191,64,128,255]`, enabling only that exact tenth hash
- completed: rerun Full Warehouse/Franka with the ten-hash gate; the trace logged exactly 200 execution-enabled, the expected 3 compile-only FP64 pipelines and one 64-bit-atomic skip, ROS 2 Jazzy, `app ready`, 824 Meshes/67 unique textures, an 824-instance Metal TLAS, a visually checked textured 1440x900 Warehouse/Franka Full frame, zero targeted Render Graph/multiTick/Sensor/Hydra/format failures, and exit zero
- completed: dispatch captured `75cdce75f76c7184` with its real panoramic depth/color textures, independent samplers, texel buffers, four-element SSBO descriptor array, and atomic counter buffer on Apple M4; changing only binary64 camera-origin y from `-2` to the enabled `-y=0` plane reduces the checked four-level counters from `[64,32,128,800]` to `[32,32,64,384]`, enabling only that exact eleventh hash
- completed: rerun Full Warehouse/Franka with the eleven-hash gate; the trace logged exactly 201 execution-enabled, the expected 2 compile-only FP64 pipelines and one 64-bit-atomic skip, ROS 2 Jazzy, `app ready`, 824 Meshes/67 unique textures, an 824-instance Metal TLAS, a visually checked textured 1440x900 Warehouse/Franka Full frame, zero targeted Render Graph/multiTick/Sensor/Hydra/format failures, and exit zero
- completed: add `VK_FORMAT_R8G8B8A8_SNORM` / Metal `rgba8Snorm` ordinary-image transport and dispatch captured `d1b78c3914cb1874` over a 3x3 depth field with every descriptor bound; signed-negative neighbor validity makes both software-FP64 clipping sites execute, while changing only binary64 camera-origin y preserves all nine checked depth pixels at `[128,0,0,0]` and all nine post-neighbor markers at `[64,0,0,0]`, matching the generated MSL proof that both clipped ray lengths are output-dead and enabling only that exact twelfth hash
- completed: rerun Full Warehouse/Franka with the twelve-hash gate; the trace logged exactly 202 execution-enabled, the expected one compile-only FP64 pipeline and one 64-bit-atomic skip, ROS 2 Jazzy, `app ready`, 824 Meshes/67 unique textures, an 824-instance Metal TLAS, a visually checked textured 1440x900 Warehouse/Franka Full frame, zero targeted Render Graph/multiTick/Sensor/Hydra/format failures, and exit zero
- completed: dispatch captured `0a553b2a8825d0ff` over a 3x3 temporal-consistency field with all four descriptor sets bound; its current-ray and four-neighbor FP64 clipping loops both execute, while changing only both binary64 camera-origin z values from `-2` to `0` preserves all nine checked blue `[0,0,255,255]` outputs, matching the generated MSL proof that each clipped length is output-dead and enabling only that exact thirteenth hash
- completed: rerun Full Warehouse/Franka with the thirteen-hash gate; the trace logged exactly 203 execution-enabled, zero compile-only FP64 pipelines, the expected single 64-bit-atomic skip, ROS 2 Jazzy, `app ready`, 824 Meshes/67 unique textures, an 824-instance Metal TLAS, a visually checked textured 1440x900 Warehouse/Franka Full frame, zero targeted Render Graph/multiTick/Sensor/Hydra/format failures, and exit zero
- completed: prove with the Apple M4 runtime compiler that native MSL `atomic_ulong` is unavailable, then implement a correctness-preserving serialized lowering for coordination-free 64-bit compare-exchange; direct Metal buffer/texture/sampler indices now carry Vulkan sets 8 and above, same-binding aliases remain intact, and the actual `f72cb1589be7be96` fixture verifies `counter=1`, one occupied slot, and inserted key `0x17f0000010001`
- completed: rerun Full Warehouse/Franka with every captured compute shader enabled; the trace logged exactly 204 execution-enabled, zero compile-only and zero skipped pipelines, ROS 2 Jazzy, `app ready`, 824 Meshes/67 unique textures, an 824-instance Metal TLAS, a visually checked textured 1440x900 frame, zero targeted Render Graph/multiTick/Sensor/Hydra/format failures, and exit zero
- next: validate the ordinary no-demo Simple Grid startup and then expand material/shader, coordinated/shared-memory compute, remaining 64-bit atomic, nested-instancing/deformation, and native RTX sensor semantics without weakening the existing correctness checks
- completed: traverse native USD scenegraph instance proxies and emit each composed proxy path as a separate scene-state Mesh/world-transform record; a Full demo verifies two references to one abstract pyramid prototype as `instanceProxyMeshes=2`, four Metal BLASes, and one four-instance TLAS
- completed: expand bounded current-time `UsdGeomPointInstancer` prototypes through OpenUSD's own prototype-index, position, quaternion orientation, scale, prototype-root/instancer transform, ID, inactive/invisible mask semantics; a Full demo authored four points, masked one, reported `pointInstances=3 pointInstanceMeshes=3`, and built the expected seven-Mesh TLAS
- completed: convert `omni.timeline` seconds through the stage time-code rate and sample camera, transforms, Mesh topology/points/normals/UVs/display color, lights, native instance proxies, and PointInstancer attributes at that time; `--animate-demo` loops the explicit demo stage, publishes only changed atomic payloads, and refreshes the fallback TLAS while content-identical Metal BLAS/material resources are reused. A 300-frame Full run reached scene sequence 52, passed 94 seconds, exited zero, and reported none of the targeted Render Graph/multiTick/Sensor/Hydra/crash/task-group failures
- next: preserve nested instancers and per-instance primvar overrides, translate animated material inputs, and make skinning/blend-shape/topology-changing deformation robust in Full
- completed: preserve per-triangle geometric face normals at BLAS build time, transform them into world space for each TLAS instance, and use them for bounded SphereLight/DistantLight diffuse response; a rotated-instance Metal regression proves lighting differs from the former view-facing fallback
- completed: resolve USD Mesh `constant`, `uniform`, `vertex`, `varying`, and `faceVarying` authored normals to triangulated corners in scene-state v6+, transform them per instance, and barycentrically interpolate them in the Metal ray hit shader; a planar gradient regression verifies the interleaved position/normal path
- completed: trace a second ray through the real TLAS for SphereLight and DistantLight visibility; an occluder regression proves the primary hit stays fixed while direct illumination drops, and the Full demo visibly casts the Cube shadow onto Ground
- completed: direct `UsdPreviewSurface`/OmniPBR roughness and metallic constants reach a per-instance Metal material buffer and bounded dielectric-to-metal diffuse/specular response; a real-ray regression distinguishes rough dielectric from smooth metal and Full logs verify Cube 0.32 versus Ground 0.82 roughness
- completed: direct Preview/OmniPBR emissive color/intensity reaches the per-instance Metal material record and is added without a light; a no-light regression verifies green self-emission and Full v9 logs Cube `(0.060,0.015,0.000)x1.000`
- completed: scene-state v10 resolves file-backed roughness/metallic/emission connections sharing one UV primvar, retains scalar channel selectors, creates separate Metal images plus a validated per-material descriptor, and a spatial emission-map regression rejects average-color substitution; Full logs two 8x8 Cube parameter maps
- completed: scene-state v11 introduced one connected tangent-space normal image, derives tangent/bitangent vectors from real triangle positions and UV derivatives, transports vertex format 6, and orthogonalizes the transformed TBN basis in Metal; v12 added a separate static sequence and v13 preserves it
- completed: scene-state v15 retains separate camera and static Mesh/light/material sequences; a real Full Warehouse plus Franka run advanced camera sequence 1 to 2, preserved mesh sequence 1, reused the existing 824-instance Metal TLAS, and avoided a second full parse/rematch
- completed: keep a complete scene Mesh when individual triangles have degenerate geometry normals or repeated/zero-area normal-map UVs by generating finite stable fallback bases; Full now reports `sceneGeometryMeshes=824` with zero native bounds matches or missing Mesh matches
- completed: resolve a standard connected `UsdTransform2d` chain, apply its authored scale then counter-clockwise degree rotation then translation to triangulated corner UVs, require every map on the bounded material to share that complete UV source, and verify the non-identity demo network in real Full with `uvTransforms=1`
- completed: normalize the 12 official Simple Warehouse source-asset MDL modules, including channel-mask blends, pushcart masks, tint/desaturation, sign/barcode composition, ORM roughness ranges, normal strength, and sRGB color handling; scene-state v15 deduplicates images globally and preserves 824 Warehouse-plus-Franka material parts with independent 192 MiB geometry and 128 MiB texture budgets, 512px transport images, and 126 texture slots
- completed: preserve the official Barcode texture alpha and bake WallBoard `AlphaSelection` into base alpha, tag 12 real Warehouse Mesh records, and re-intersect tagged Metal hits below `0.3333`; a real Metal regression proves a transparent front triangle reveals the opaque triangle behind it
- completed: scene-state v15 and `MBM1 v3` carry standard `UsdPreviewSurface`/OmniPBR opacity and opacityThreshold, compose a compatible connected opacity texture into base alpha, and tag only those materials for visibility testing. The Full demo's opacity 0.25/threshold 0.5 canopy is absent in the checked Metal frame while unrelated texture alpha stays opaque
- completed: split `materialBind` GeomSubsets into disjoint face records. Franka link0's six material parts cover all 20,264 faces exactly, so Aluminum, PlasticWhite, PlasticGray, PlasticBlack, and EmissiveBlue no longer repaint a complete link
- completed: cache static Mesh/material/light scene-state bytes until an OpenUSD objects-changed notice, retain live camera-only publication, and keep adaptive hard-shadow/ray-pixel execution as an explicit performance opt-in; correctness-first one-ray-per-pixel plus hard shadows is the default. The latest 824-instance Full run held about 1.9-2.0 FPS, and Orbit published a cached update with `meshSequence=1` and reused the TLAS without a second Mesh rematch
- completed: source-over composite standard zero-threshold fractional opacity front-to-back for up to 64 Metal ray hits in linear color. A real Full + ROS 2 run published opacity 0.35, rendered the blue panel with the textured cube and floor visible behind it, preserved the positive-threshold cutout, and exited zero
- completed: make SphereLight and DistantLight shadow rays skip sub-threshold cutouts and multiply standard fractional opacity through up to 64 blockers; real Metal regressions distinguish fully lit, opacity-0.5, and opaque-blocked receivers for both light types
- completed: accept the first USD RectLight or DiskLight in the existing bounded positional-light slot, mapping its authored center and equal-area radius to hard Metal lighting. A real Full run published and visibly lit from a RectLight while preserving DistantLight, DomeLight, Full UI, and ROS 2 startup
- completed: protocol 1.17 and scene-state v15 append at most thirteen validated additional-light records while keeping the legacy inline slots. The stage publishes up to eight positional, four distant, and four dome lights; Metal accumulates all records with opacity-aware hard shadows for positional and distant lights. A real Full/ROS2 run published DiskLight, second DistantLight, and second DomeLight records and reported `lights=2/2/2`
- completed: protocol 1.18 and scene-state v16 replace the split fixed-plus-additional representation with one complete list of at most sixteen 80-byte records. RectLight/DiskLight now preserve transformed in-plane axes and half extents; Metal evaluates four deterministic surface samples, OpenUSD's one-sided `-Z` emission convention, and independent opacity-aware shadow visibility. The regression distinguishes back-side unlit, partially occluded, and fully visible receivers, and a real Full/ROS 2 run published RectLight, DiskLight, two DistantLights, and two DomeLights with host `lights=2/2/2`
- completed: interpret DistantLight `angle` as OpenUSD's full angular diameter over `[0, 360)`. Zero remains one parallel ray; nonzero values generate four deterministic spherical-cap directions with separate opacity-aware shadow traversals. A Metal regression uses a blocker that misses the center ray but covers one 30-degree cone sample and distinguishes partial from full visibility. The rebuilt real Full/ROS 2 run published its 1-degree KeyLight, executed `lights=2/2/2`, retained the complete UI and visible scene, enabled all 204 compute pipelines, and exited zero without targeted Render Graph/multiTick/Sensor/Hydra failures
- completed: protocol 1.19 and scene-state v17 add CylinderLight schema 6 without enlarging the 80-byte light record. The publisher carries transformed local-X half length and local-Y/local-Z radial extents; Metal evaluates two axial positions by four radial angles with outward elliptical-cylinder normals and independent opacity-aware shadows. `treatAsLine` uses two axial samples. A Metal regression distinguishes partial from full visibility
- completed: protocol 1.20 and scene-state v18 expand the light record to 120 bytes for explicitly applied `UsdLuxShapingAPI`. The stage publishes the world emission axis, cone angle, softness, focus, and focusTint; Metal evaluates cone falloff and focus color per emitter sample. A dedicated regression distinguishes outside, softened, hard-edged, and focus-tinted results. The real Full/ROS 2 run published `/World/SpotLight`, reported `lights=4/2/2`, enabled all 204 compute pipelines, displayed the checked Full frame, and exited zero
- next: translate broader hit/miss/material networks, colored absorption/refraction/volume semantics, authored tangent semantics, remaining Geometry/Portal light schemas, light textures and IES profiles, broader raster commands, and NVIDIA's native Replicator/RTX sensor arrays

## M5 — CUDA startup compatibility (prototype startup slice completed)

- traced real CUDA Driver API and private export-table calls from Kit, PhysX, and Warp startup
- implemented the tested identity, context, allocation, copy, stream, event, module, and export-table startup surface
- Warp discovers one CUDA compatibility device during real Isaac Sim startup
- next: evaluate PTX translation and Metal execution feasibility and legal/technical constraints
- explicitly gate unsupported APIs

## M6 — Real Isaac Sim startup (Base and Full profiles completed)

- official `isaacsim.exp.base.kit` experience reports `app ready`
- verified both finite smoke-test shutdown and continued running state after readiness; the launcher now propagates a later container-init crash instead of treating readiness alone as success
- repeatable lifecycle is implemented in `scripts/run-isaac-sim.sh`
- targeted ARM virtual-counter compatibility removes the Apple-VM backwards-TSC abort
- official Full experience reports `Isaac Sim Full App is loaded`, reaches `app ready`, displays the real UI and Simple Grid scene presentation, and exits zero in finite validation
- protocol 1.18 retains Kit's normal no-USD `World`/`Environment` stage and draws the camera-aware perspective guide plus X/Y axes into only the 1280×720 landscape viewport ring. The checked 1440×900 Full frame preserved readable menus, Stage, Property, Content, and XYZ UI; ROS 2 Jazzy loaded, all 204 Metal compute pipelines were enabled, the font/icon atlas was excluded, and targeted Render Graph/multiTick/Sensor/Hydra/memory-map failures remained zero
- completed: reflect the SPIR-V `PushConstant` `OpVariable` result ID/name into its exact Metal `[[buffer(N)]]` parameter instead of taking the first constant signature. A synthetic direct-set-8-UBO-before-push regression passes, host tests are 41/41, and the rebuilt Full/ROS2 run remains 204 enabled, 0 compile-only/skipped, app ready, and zero targeted push/Render Graph/multiTick/Sensor/Hydra/memory-map failures
- Full always enables and validates bundled ROS 2 Jazzy core, internal `rclpy`, and `isaacsim.ros2.bridge`; `--ros2` is a compatibility no-op
- RGBA8 sRGB, BC3 sRGB, and BC5 UNORM sparse images now preserve Vulkan transfer order, upload through aligned Metal blits, and pass map/upload/unmap on Apple M4
- the 2026-08-11 Full/ROS2 protocol-1.18 rerun reported Apple M4 with 24,142 MB, enabled all 204 captured compute pipelines, published a scene-state-v16 complete light list with oriented RectLight/DiskLight plus `lights=2/2/2`, produced a checked textured 1440x900 Full frame with transparency and shadows, removed the former format-43 sparse-image error, and exited zero with no targeted Render Graph/multiTick/Sensor/Hydra failures
- the 2026-08-11 Full/ROS2 protocol-1.19 rerun reported Apple M4 with 24,142 MB, enabled all 204 captured compute pipelines, published a scene-state-v17 complete seven-light list with oriented RectLight/DiskLight/CylinderLight plus `lights=3/2/2`, produced a checked textured 1440x900 Full frame with transparency and shadows, and exited zero with no targeted Render Graph/multiTick/Sensor/Hydra failures
- the 2026-08-11 Full/ROS2 protocol-1.20 rerun reported Apple M4 with 24,142 MB, enabled all 204 captured compute pipelines, published a scene-state-v18 complete eight-light list including a shaped SphereLight plus `lights=4/2/2`, produced a checked textured 1440x900 Full frame with transparency and shadows, and exited zero. Only the three expected NVIDIA-only NGX/OptiX errors remained
- official Simple Warehouse plus composed Franka Panda reaches `app ready`, builds an 824-Mesh Metal TLAS including 44 Franka material parts, displays the real Full workspace and correctly separated white/gray/aluminum robot materials, and keeps the targeted texture-upload/Render Graph/Hydra failures at zero

GPU PhysX correctness, broader CPU PhysX workloads, general RTX/Hydra shader/material/camera-sensor equivalence, long-running Full workload stability, and general CUDA compatibility remain research questions, not scheduled promises. The completed scene-validation milestone displays multiple authored visible USD Meshes, native scenegraph instance proxies, and bounded current-time PointInstancer expansions through Metal with their composed world transforms, authored interpolated or geometric fallback normals, tangent-space normal maps, transformed UVs, opacity-aware positional/DistantLight shadows, oriented four-sample RectLight/DiskLight response, applied ShapingAPI cone/focus response, active Kit camera, bounded positional/Distant/Dome light arrays, real Simple Grid diffuse texture, and one Camera Render Product file contract. It still uses a fallback TLAS and simplified bounded light response; nested instancing, per-instance primvar overrides, animated material inputs, robust skinning/deformation, remaining light schemas, light textures/IES profiles, and NVIDIA's native RGB annotator or other RTX sensors are not equivalent.
